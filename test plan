verify_repo OK; required categories/commands/templates are present. What’s left before E2E in another repo:
- Run `bash scripts/agents_refresh.sh` if you want vendor refresh + cheatsheet regen (optional if vendors unchanged).
- Link canonical `.agents` into the target repo (dotagents) and confirm `.agents/commands` + `.agents/skills` resolve to this repo.
- Keep `.agents/tasks` local in the target repo; `wf-shape`/`wf-ralph` write `.agents/tasks/prd-<slug>.json`.
- Ensure the target repo has pnpm scripts or `docs/verify.md`; `verify` in `.agents/commands/verify.md` will NO-GO otherwise.
- Ensure the target repo can create `docs/shape`, `docs/dev`, `docs/review`, `docs/release`, and `docs/learnings.md`.

**Test Plan**
- **Smoke 1**: Canonical integrity: run `bash scripts/verify_repo.sh` (optional `bash scripts/agents_refresh.sh`); expect OK.
- **Smoke 2**: Link visibility: in target repo, confirm `.agents/commands/wf-shape.md` and `.agents/skills/shape/create-prd/SKILL.md` resolve to canonical; run `/prompts:wf-shape smoke` and expect `docs/shape/smoke/prd.md` + `.agents/tasks/prd-smoke.json`.
- **Smoke 3**: Verify path: run `/verify` in a repo with pnpm scripts or `docs/verify.md`; expect GO/NO-GO summary.

- **E2E 1**: Happy path small feature: `wf-shape` -> `wf-develop` -> `wf-review` -> `wf-release`; expect `docs/shape/<slug>/prd.md`, `docs/dev/<slug>/dev-log.md`, `docs/review/<slug>/review.md`, `docs/release/<slug>/release.md`, optional `docs/learnings.md` append.
- **E2E 2**: No-Go path: force a failing test; run `wf-develop` or `verify`; expect NO-GO recorded in `docs/dev/<slug>/dev-log.md` with failure evidence.
- **E2E 3**: Ralph loop: create `.agents/tasks/prd-<slug>.json`, run `wf-ralph` with 2 iterations; expect `.ralph/` state, optional `docs/dev/<slug>/ralph-log.md`, and GO/NO-GO after `verify`.

If you want, I can help pick a tiny feature and a target repo that best matches the pnpm verify ladder.
